BlindBoxDto:
    type: object
    properties:
      blindBoxId:
        type: integer
        format: int64
      brandId:
        type: integer
        format: int64
      name:
        type: string
      description:
        type: string
      currentPrice:
        type: number
        format: double
      promotionalCampaignId:
        type: integer
        format: int64
      isVisible:
        type: boolean
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
      images:
        type: array
        items:
          $ref: "image.yml#/ImageDto"
      # Add any additional properties specific to BlindBoxDto here