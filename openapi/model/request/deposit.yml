DepositDto:
  type: object
  properties:
    accountId:
      type: integer
      format: int64
    amount:
      type: number
      format: double
    paymentMethod:
      type: string
      enum: [VNPAY, PAYOS]
