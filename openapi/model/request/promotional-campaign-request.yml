PromotionalCampaignRequestDto:
  type: object
  properties:
    campaignId:
      type: integer
      format: int64
    title:
      type: string
    description:
      type: string
    startDate:
      type: string
      format: date-time
    endDate:
      type: string
      format: date-time
    discountRate:
      type: number
      format: double
    isVisible:
      type: boolean
    blindBoxIds:
        type: array
        items:
            type: integer
            format: int64
