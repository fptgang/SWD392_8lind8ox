VideoDto:
  type: object
  properties:
    videoId:
      type: integer
      format: int64
    accountId:
      type: integer
      format: int64
    orderDetailId:
      type: integer
      format: int64
    url:
      type: string
    description:
      type: string
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    isVisible:
      type: boolean
