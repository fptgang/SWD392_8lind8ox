PackDto:
  type: object
  properties:
    packId:
      type: integer
      format: int64
    name:
      type: string
    description:
      type: string
    quantity:
      type: integer
      format: int32
    price:
      type: number
      format: double
    isVisible:
      type: boolean
    createdDate:
      type: string
      format: date-time
    updatedDate:
      type: string
      format: date-time
    blindBoxes:
      type: array
      items:
        $ref: "blindbox.yml#/BlindBoxDto"
    orderDetails:
      type: array
      items:
        $ref: "order-detail.yml#/OrderDetailDto"
