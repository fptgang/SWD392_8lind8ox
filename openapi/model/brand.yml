BrandDto:
  type: object
  properties:
    brandId:
      type: integer
      format: int64
    name:
      type: string
    description:
      type: string
    isVisible:
      type: boolean
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    blindBoxes:
      type: array
      items:
        type: integer
        format: int64