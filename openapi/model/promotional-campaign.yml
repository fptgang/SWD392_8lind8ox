PromotionalCampaignDto:
  type: object
  properties:
    campaignId:
      type: integer
      format: int64
    creatorId:
      type: integer
      format: int64
    title:
      type: string
    description:
      type: string
    startDate:
      type: string
      format: date-time
    endDate:
      type: string
      format: date-time
    discountRate:
      type: number
      format: double
    code:
      type: string
    quantity:
      type: integer
      format: int64
    isVisible:
      type: boolean
    createdDate:
      type: string
      format: date-time
    updatedDate:
      type: string
      format: date-time
    blindBoxes:
      type: array
      items:
        $ref: "blindbox.yml#/BlindBoxDto"
