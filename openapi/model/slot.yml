SlotDto:
  type: object
  properties:
    slotId:
      type: integer
      format: int64
    position:
      type: integer
      format: int32
    isOpened:
      type: boolean
    isVisible:
      type: boolean
    openedAt:
      type: string
      format: date-time
    toy:
      $ref: "toy.yml#/ToyDto"
    setId:
      type: integer
      format: int64
    createdAt:
      type: string
      format: date-time
    updatedAt:
      type: string
      format: date-time
    video:
      $ref: "video.yml#/VideoDto"