ImageDto:
  type: object
  properties:
    imageId:
      type: integer
      format: int64
    uploaderId:
      type: integer
      format: int64
    productId:
      type: integer
      format: int64
    imageUrl:
      type: string
    isVisible:
      type: boolean
    createdAt:
      type: string
      format: date-time

