ImageDto:
  type: object
  properties:
    imageId:
      type: integer
      format: int64
    uploader:
      $ref: "account.yml#/AccountDto"
    blindBoxId:
      type: integer
      format: int64
    toyId:
      type: integer
      format: int64
    skuId:
      type: integer
      format: int64
    imageUrl:
      type: string
    isVisible:
      type: boolean
    createdAt:
      type: string
      format: date-time
